require 'spec_helper'

describe Internet::Sampler do
  it 'has a version number' do
    expect(Internet::Sampler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
